<UserControl x:Class="OnvifDeviceManager.Wpf.Views.CredentialManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:OnvifDeviceManager.ViewModels;assembly=OnvifDeviceManager.Core"
             Background="Transparent">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24">
            <TextBlock Text="Credential Manager" FontSize="24" FontWeight="SemiBold" Foreground="White" />
            <TextBlock Text="Manage per-device and group credentials for ONVIF cameras" FontSize="13" Foreground="#B0BEC5" Margin="0,4,0,24" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Device Credentials -->
                <StackPanel Grid.Column="0">
                    <Border Style="{StaticResource Card}" Margin="0,0,0,16">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <TextBlock Text="Device Credentials" FontSize="16" FontWeight="SemiBold" Foreground="White" />
                                <TextBlock Text="{Binding Credentials.Count, StringFormat='{}{0} saved'}" FontSize="11" Foreground="#607D8B" HorizontalAlignment="Right" VerticalAlignment="Center" />
                            </Grid>
                            <ListBox Style="{StaticResource ModernLB}" ItemContainerStyle="{StaticResource ModernLBI}"
                                     ItemsSource="{Binding Credentials}" SelectedItem="{Binding SelectedCredential}" MaxHeight="200">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock Text="{Binding DeviceAddress}" FontWeight="SemiBold" Foreground="White" />
                                                <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                                    <TextBlock Text="{Binding Username}" FontSize="11" Foreground="#00D4AA" />
                                                    <TextBlock Text="{Binding DeviceName, StringFormat=' ({0})'}" FontSize="11" Foreground="#607D8B" />
                                                </StackPanel>
                                            </StackPanel>
                                            <TextBlock Grid.Column="1" Text="{Binding LastUsed, StringFormat='yyyy-MM-dd'}" FontSize="11" Foreground="#607D8B" VerticalAlignment="Center" />
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                            <Button Style="{StaticResource DangerBtn}" Content="Delete Selected" Command="{Binding DeleteDeviceCredentialCommand}" Margin="0,8,0,0" Padding="12,6" HorizontalAlignment="Left" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Text="Add / Update Device Credential" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,12" />
                            <TextBlock Text="Device Address (IP or hostname)" FontSize="11" Foreground="#607D8B" Margin="0,0,0,4" />
                            <TextBox Style="{StaticResource ModernTB}" Text="{Binding NewDeviceAddress, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <TextBlock Text="Username" FontSize="11" Foreground="#607D8B" Margin="0,0,0,4" />
                                <TextBlock Grid.Column="2" Text="Password" FontSize="11" Foreground="#607D8B" Margin="0,0,0,4" />
                                <TextBox Grid.Row="1" Style="{StaticResource ModernTB}" Text="{Binding NewDeviceUsername, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBox Grid.Row="1" Grid.Column="2" Style="{StaticResource ModernTB}" Text="{Binding NewDevicePassword, UpdateSourceTrigger=PropertyChanged}" />
                            </Grid>
                            <Button Style="{StaticResource PrimaryBtn}" Content="Save Credential" Command="{Binding SaveDeviceCredentialCommand}" Padding="12,8" />
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Groups -->
                <StackPanel Grid.Column="2">
                    <Border Style="{StaticResource Card}" Margin="0,0,0,16">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <TextBlock Text="Device Groups" FontSize="16" FontWeight="SemiBold" Foreground="White" />
                                <TextBlock Text="{Binding Groups.Count, StringFormat='{}{0} group(s)'}" FontSize="11" Foreground="#607D8B" HorizontalAlignment="Right" VerticalAlignment="Center" />
                            </Grid>
                            <ListBox Style="{StaticResource ModernLB}" ItemContainerStyle="{StaticResource ModernLBI}"
                                     ItemsSource="{Binding Groups}" SelectedItem="{Binding SelectedGroup}" MaxHeight="150">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="White" />
                                            <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                                <TextBlock Text="{Binding Username}" FontSize="11" Foreground="#00D4AA" />
                                                <TextBlock Text="{Binding DeviceAddresses.Count, StringFormat=' \u2022 {0} device(s)'}" FontSize="11" Foreground="#607D8B" />
                                            </StackPanel>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>

                            <!-- Devices in group -->
                            <Border Background="#0D1B2A" CornerRadius="8" Padding="12" Margin="0,8,0,0">
                                <StackPanel>
                                    <TextBlock Text="Devices in this group:" FontSize="11" FontWeight="Bold" Foreground="#607D8B" Margin="0,0,0,6" />
                                    <ItemsControl ItemsSource="{Binding SelectedGroupDevices}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,2">
                                                    <TextBlock Text="{Binding}" FontSize="12" Foreground="#00D4AA" FontFamily="Cascadia Code, Consolas" VerticalAlignment="Center" />
                                                    <Button Style="{StaticResource DangerBtn}" Content="X" Padding="6,2" HorizontalAlignment="Right"
                                                            Command="{Binding DataContext.RemoveDeviceFromGroupCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            CommandParameter="{Binding}" />
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <TextBlock Text="Add device by IP address (e.g. 192.168.1.100):" FontSize="11" Foreground="#607D8B" Margin="0,8,0,4" />
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBox Style="{StaticResource ModernTB}" Text="{Binding AddToGroupAddress, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0" />
                                        <Button Grid.Column="1" Style="{StaticResource PrimaryBtn}" Content="Add Device" Command="{Binding AddDeviceToGroupCommand}" Padding="12,6" />
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <Button Style="{StaticResource SecondaryBtn}" Content="Update Group" Command="{Binding UpdateGroupCommand}" Padding="12,6" Margin="0,0,8,0" />
                                <Button Style="{StaticResource DangerBtn}" Content="Delete Group" Command="{Binding DeleteGroupCommand}" Padding="12,6" />
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Text="Create New Group" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,12" />
                            <TextBlock Text="Group Name" FontSize="11" Foreground="#607D8B" Margin="0,0,0,4" />
                            <TextBox Style="{StaticResource ModernTB}" Text="{Binding NewGroupName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <TextBlock Text="Shared Username" FontSize="11" Foreground="#607D8B" Margin="0,0,0,4" />
                                <TextBlock Grid.Column="2" Text="Shared Password" FontSize="11" Foreground="#607D8B" Margin="0,0,0,4" />
                                <TextBox Grid.Row="1" Style="{StaticResource ModernTB}" Text="{Binding NewGroupUsername, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBox Grid.Row="1" Grid.Column="2" Style="{StaticResource ModernTB}" Text="{Binding NewGroupPassword, UpdateSourceTrigger=PropertyChanged}" />
                            </Grid>
                            <Button Style="{StaticResource PrimaryBtn}" Content="Create Group" Command="{Binding CreateGroupCommand}" Padding="12,8" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <Border Margin="0,16,0,0" Padding="12,8" Background="#1E2A4A" CornerRadius="12">
                <TextBlock Text="{Binding StatusText}" FontSize="11" Foreground="#607D8B" />
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
